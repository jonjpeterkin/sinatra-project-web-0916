<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <p>
      <%unless @loc == "restaurants"%>
        <a href="/restaurants"><strong>Find Restaurants</a> | </strong>
      <%end%>
      <%unless @loc == "users"%>
        <a href="/users"><strong>User List</a> | </strong>
      <%end%>
      <a href="/restaurants/new"><strong>Add a Restaurant</a> | </strong>
      <a href="/users/signin"><strong>Sign In</a></strong>
      <i><% if User.signed_in %>
      <%="(signed in as #{User.signed_in})"%>
      <%else%>
      <%="(not signed in)" %>
      <%end%>
      </i>
    </p>
    <% if flash.has?(:message) %>
      -- <strong><%= flash[:message] %></strong> --
    <% end %>
    <%=yield%>
  </body>
</html>
